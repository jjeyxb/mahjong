# 開發工具 — 安裝於主環境 (Python 3.14)
#   .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/pip install -e . --no-deps
#
# 第二行不能省:tests/conftest.py 直接 `import mia`,這裡沒有任何東西會把
# src/ 插進 sys.path。pytest 能找到 mia 全靠這個 editable install。
# --no-deps 是因為相依已經在上面 requirements.txt 裝過,重複解析沒有意義。

-r requirements.txt

# --- 測試 ---
pytest>=9.0
pytest-qt>=4.4              # PySide6 元件測試
pytest-cov>=6.0

# --- 靜態檢查 ---
ruff>=0.9
mypy>=1.14
types-PyYAML                 # config/loader.py 用到 yaml,mypy 少了它會炸 import-untyped
types-protobuf                # groundtruth/ 的 liqi.proto 產物,同上

# --- CV 開發輔助 ---
scikit-image>=0.26          # 影像相似度指標 (SSIM),模板品質評估用
matplotlib>=3.10            # eval 報告的混淆矩陣 / 準確率圖表
